import React, { useEffect } from "react";
//import { useHistory } from "react-router-dom";

const BootsHome = () => {
  // if (!localStorage.getItem("token")) push("/home");

  return (
    <>
      <div>
        <h1 className="center_div"> Login Successfully, Wellcome to Home</h1>
      </div>
    </>
  );
};

export default BootsHome;
